version: '3.8'
services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: 'ciel'
      POSTGRES_PASSWORD: 'ciel'
      POSTGRES_DB: 'ciel'
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: